<div class="mx-auto w-full">
  <%= link_to sanitize(t("regresar_a_facturas")), invoices_path, class: "back-button" %>
  <h1 class="text-2xl md:text-3xl text-slate-800 font-bold mt-2"><%= ("Edit Invoice")%> ✨</h1>

  <%= render "form", invoice: @invoice %>
  
</div>
